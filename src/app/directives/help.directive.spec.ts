import { HelpDirective } from './help.directive';

describe('HelpDirective', () => {
  it('should create an instance', () => {
    const t:any = '';
    const directive = new HelpDirective(t);
    expect(directive).toBeTruthy();
  });
});
